# 콩닥 소셜 로그인(카카오·네이버·구글) 안 되는 문제 — 원인·해결

## 원인 (실측)
카카오·네이버·구글이 **한꺼번에** 실패 = 제공자별 문제가 아니라 공통 원인 = **콜백 URL/도메인 설정**.

1. **NEXTAUTH_URL 충돌 (핵심):**
   - `.env` = `https://kongdak.kr` (정상)
   - `.env.local` = `https://thekdestiny.com` (옛 도메인) ← Next.js는 `.env.local`이 `.env`를 덮어쓴다.
   - NextAuth가 이 값으로 콜백 주소를 만들어서, 로그인 후 옛 죽은 도메인으로 돌아가 셋 다 실패.
2. **제공자 콘솔에 kongdak.kr 콜백 URL 미등록 가능성** (도메인이 바뀌었으므로 갱신 필요).
3. (흔한 함정) 제공자 앱이 **개발/테스트 모드**면 등록된 계정만 로그인됨.

---

## 해결 1 — NEXTAUTH_URL 바로잡기 (환경별)
NextAuth 콜백은 **테스트하는 도메인과 정확히 일치**해야 한다.

- **로컬(localhost:3000)에서 테스트:** `.env.local` 의
  `NEXTAUTH_URL=http://localhost:3000`  로 수정 → dev 서버 재시작(`npm run dev`).
- **배포(kongdak.kr)에서 테스트:** 서버가 쓰는 `.env` 의
  `NEXTAUTH_URL=https://kongdak.kr` (이미 정상). 단 **서버에 옛 `.env.local`(thekdestiny.com)이 남아 있지 않은지 확인** — 있으면 그게 덮어쓴다. 서버는 `.env.local` 없이 `.env`만 두거나, `.env.local`도 kongdak.kr로.
- 어떤 경우든 `thekdestiny.com` 값은 완전히 제거.

> 팁: 로컬·배포를 오가며 테스트하면 값 때문에 헷갈린다. **배포(kongdak.kr) 기준으로 통일**해서 테스트하는 걸 추천.

## 해결 2 — 각 제공자 콘솔에 콜백 URL 등록 (형이 직접)
NextAuth v4 콜백 경로 형식: `/api/auth/callback/{provider}`

### 구글 (Google Cloud Console → API 및 서비스 → 사용자 인증 정보 → OAuth 클라이언트)
- **승인된 리디렉션 URI**에 추가:
  - `https://kongdak.kr/api/auth/callback/google`
  - (로컬도 쓰면) `http://localhost:3000/api/auth/callback/google`
- **승인된 자바스크립트 원본**: `https://kongdak.kr` (+ `http://localhost:3000`)
- OAuth 동의 화면이 "테스트" 상태면 → 테스트 사용자에 본인 계정 추가하거나 앱 게시.

### 카카오 (Kakao Developers → 내 애플리케이션)
- **카카오 로그인 → 활성화 ON**
- **Redirect URI** 등록:
  - `https://kongdak.kr/api/auth/callback/kakao` (+ 로컬 `http://localhost:3000/api/auth/callback/kakao`)
- **플랫폼 → Web → 사이트 도메인**: `https://kongdak.kr`
- **동의 항목**: 닉네임/프로필/이메일 등 필요한 항목 사용 설정(이메일은 검수 필요할 수 있음).

### 네이버 (Naver Developers → 애플리케이션 → API 설정)
- **서비스 URL**: `https://kongdak.kr`
- **네이버 로그인 Callback URL**: `https://kongdak.kr/api/auth/callback/naver` (+ 로컬)
- ⚠️ 네이버 앱이 **"개발 중"** 상태면 **등록된 멤버(테스터) 계정만** 로그인됨. 본인 네이버 아이디를 멤버로 등록하거나 검수 요청.

## 해결 3 — Cloudflare 확인 (해당 시)
- SSL 모드가 **Full (strict)** 인지 재확인(Flexible이면 리다이렉트 꼬여 콜백 실패 가능).
- 로그인 콜백 경로(`/api/auth/*`)는 동적이라 캐시 안 됨(기본값이면 문제 없음). 커스텀 캐시 규칙을 넣었다면 `/api/*` 제외.

---

## 적용 후 확인
1. 위 1·2(필요시 3) 적용 → 재빌드/재시작 → 캐시 비우기(Cloudflare Purge, 배포 시).
2. 로그인 클릭 → 제공자 동의 화면 → **kongdak.kr로 정상 복귀 + 로그인 완료** 확인.
3. 여전히 안 되면, 실패 순간의 **주소창 URL과 에러 메시지**(보통 `/api/auth/error?error=...`)를 캡처해서 공유. error 값(예: `OAuthCallback`, `Configuration`, `redirect_uri_mismatch`)으로 정확히 짚을 수 있음.

## 참고: 에러별 의미
- `redirect_uri_mismatch` (구글) → 콘솔 리디렉션 URI 불일치(해결 2).
- `Configuration` → NEXTAUTH_URL/SECRET 등 서버 설정 문제(해결 1).
- `OAuthCallback` / 콜백 후 옛 도메인으로 튐 → NEXTAUTH_URL 잘못(해결 1, 지금 원인).
- `KOE006`(카카오) → 카카오 Redirect URI 미등록(해결 2).
