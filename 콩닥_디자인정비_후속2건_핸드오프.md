# 콩닥 디자인 정비 후속 2건 — Gemini 핸드오프 (3.1 Pro 권장)

**전제:** 앞선 디자인 정비(Part 1~3: 라이트 테마·네비·반응형)가 반영된 상태의 후속 수정.
**절대 규칙:** en/de/es/fr/ja.json 등 타 로케일 텍스트 파일 수정 금지(ko.json 은 활성 로케일이라 수정 가능). 궁합 엔진(lib/compatibility.ts)·결제/매칭 로직 손대지 말 것. 기존 데이터 계약(전송 값 포맷) 유지.

---

## 수정 1 — 로그인 모달(AuthModal) 잘림·옛 문구·다크 톤 정리

### 1-A. 모달이 화면 위로 잘리는 문제
`components/AuthModal.tsx` 의 오버레이/컨테이너를 **뷰포트 전체 고정 중앙 정렬 + 내부 스크롤**로 고쳐 어떤 화면 높이에서도 안 잘리게 한다.
- 최상위 래퍼: `fixed inset-0 z-[100] flex items-center justify-center p-4` (네비보다 높은 z-index). 배경 오버레이는 그 안에서 `absolute inset-0`.
- 카드: `w-full max-w-md max-h-[90vh] overflow-y-auto` 유지하되, 상·하 여백(p-4 래퍼) 덕에 상단이 절대 화면 밖으로 안 나가게. 스크롤은 카드 내부에서만.
- 카드가 네비(fixed) 아래에 갇히지 않도록, 모달은 `document.body` 기준 최상위 레이어로 렌더(필요 시 portal 또는 최상위 fixed). 열렸을 때 배경 스크롤 잠금(body overflow hidden) 권장.

### 1-B. 라이트 콩닥 톤으로 재스타일
현재 다크(`bg-white/[0.03]`+검정 오버레이+purple/gold 그라데이션+`text-white`+gold) → 라이트로.
- 오버레이: 반투명 plum/ink (예: `bg-[#2B2430]/50 backdrop-blur-sm`).
- 카드: 흰/크림 배경(`bg-white`), ink 본문, coral 포인트, 부드러운 그림자. purple-dark/gold 장식 그라데이션·Sparkles 골드는 coral/plum 계열로 교체하거나 제거.
- 소셜 버튼 브랜드색 유지: 카카오 #FEE500(검정 글씨, 최상단·1순위), 네이버 #03C75A(흰 글씨), 구글 흰 배경+테두리. 이메일 로그인은 그 아래.

### 1-C. 옛 "우주·운명" 문구 → 콩닥 톤 (ko.json "Login" 네임스페이스 교체)
아래로 정확히 교체(도사·우주·운명·에너지 컨셉 완전 제거):
```
"Login": {
  "title": "콩닥 로그인",
  "subtitle": "로그인하면 궁합 기록을 저장하고 더 편하게 이용할 수 있어요.",
  "btn_google": "Google로 계속하기",
  "or": "또는",
  "label_email": "이메일",
  "placeholder_email": "이메일을 입력하세요",
  "label_password": "비밀번호",
  "placeholder_password": "비밀번호를 입력하세요",
  "btn_signin": "로그인",
  "btn_signup": "회원가입",
  "toggle_to_signup": "아직 계정이 없으신가요? 회원가입",
  "toggle_to_signin": "이미 계정이 있으신가요? 로그인",
  "loading": "로그인 중이에요...",
  "error_general": "로그인에 실패했어요. 잠시 후 다시 시도해 주세요.",
  "success_signup": "가입 확인 링크를 이메일로 보냈어요. 메일함을 확인해 주세요.",
  "modal_title": "로그인하고 콩닥을 더 편하게"
}
```
- AuthModal 내에 하드코딩된 영어/옛 문구가 있으면 함께 콩닥 톤으로. (카카오/네이버/구글 버튼 라벨 "카카오로 계속하기 / 네이버로 계속하기 / Google로 계속하기" 정도로 통일.)

## 수정 2 — 생년월일·시간 입력 UX 개선 (`components/CompatNewClient.tsx`)
네이티브 `<input type="date">`·`<input type="time">` 는 생일 입력에 최악(캘린더가 현재 달부터 시작해 수십 년 페이징). 사주/궁합 앱 표준 방식으로 교체.

### 2-A. 생년월일
- **년 / 월 / 일 3개 드롭다운(select)** 으로 교체. 데스크톱은 3열 나란히, 모바일도 한 줄 3열(좁으면 폭 자동).
  - 년: 1930 ~ 올해(내림차순 권장), 월 1~12, 일 1~31(선택한 년·월에 맞춰 일수 보정: 2월/30·31일 처리).
- (선택 개선) 모바일에서 휠(스크롤) 피커 형태면 더 좋음 — 여력 되면 적용, 아니면 드롭다운으로 충분.

### 2-B. 태어난 시간
- 기본값 "모름"(비워두기)을 명확한 옵션으로 유지.
- 입력 시 **오전/오후 + 시(1~12) + 분(0~59, 5분 단위 권장)** 드롭다운. "모름" 선택 시 값은 null.

### 2-C. ★ 데이터 계약 유지(중요)
UI만 바꾸고 **최종 상태/전송 값 포맷은 지금과 동일하게 유지**하라:
- 생년월일 → 기존과 같은 `"YYYY-MM-DD"` 문자열(예: dateA/dateB state).
- 시간 → 기존과 같은 `"HH:mm"` **24시간** 문자열(오전/오후·12시간 UI를 내부에서 24시간으로 변환), 모름이면 `null`(현재 `time: timeA || null` 로직 그대로 동작해야 함).
- POST /api/compat, 궁합 엔진, 날짜 검증(미래/오류 400) 로직이 그대로 통과해야 한다. 값 포맷을 바꾸지 마라.

### 2-D. 반응형
- 데스크톱: 여유 있는 간격의 드롭다운. 모바일: 터치 타깃 충분히 크게(최소 44px), 3열이 좁으면 폰트/패딩 조정.

---

## 하지 말 것
- 타 로케일 파일(en/de/es/fr/ja.json) 수정 금지.
- 전송/저장 값 포맷 변경 금지(엔진·API 회귀 방지).
- NextAuth provider 설정 변경 금지 — UI/문구만.

## 완료 기준
- `npm run build` 통과, 타입 에러 0.
- 로그인 모달: 어떤 화면에서도 안 잘리고 중앙 정렬·내부 스크롤, 라이트 콩닥 톤, "우주/운명/에너지" 문구 0.
- 생년월일: 년·월·일 드롭다운으로 과거 날짜를 빠르게 선택 가능. 시간: 오전/오후·시·분 + "모름".
- 실제 궁합 계산이 기존과 동일하게 동작(값 포맷 유지).
- 데스크톱·모바일 각각 자연스러움. 변경 파일 목록 + 스크린샷(가능하면) 보고.
```
