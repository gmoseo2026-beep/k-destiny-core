# 콩닥 통합 개발 핸드오프 — 유료 서비스 + 두근이 마스코트 적용

> 한 번의 작업 세션에서 (A)유료 결제 기능 + (B)두근이 마스코트 적용을 함께 구현한다.
> 결과화면(`CompatResultClient.tsx`)은 A(페이월)·B(삼각 구도)가 같은 파일을 건드리므로 **한 번에 일관되게 편집**한다(따로 두 번 만지지 말 것).
> 사후 검수 순서: **Gemini 구현 → Opus5 보안검수(결제/권한) → Cowork 최종 체크.**

---

## 0. 절대 규칙 (공통)
- 궁합 엔진(lib/compatibility.ts)·K-loop·무료 궁합/공유 흐름 **변경 금지**. 무료 점수·키워드·공유는 그대로.
- 로케일은 **ko.json만** 수정. 다른 로케일 JSON 금지.
- Prisma는 **additive만**(기존 테이블 파괴 금지).
- 시크릿(토스 키·VAPID 등) 커밋 금지 → `.env`. 지금은 **토스 테스트 키**로 개발.
- **결제 금액·권한은 서버가 결정·검증.** 클라이언트 신뢰 금지.
- 완료 시 `npm run build` 통과.

---

# A. 유료 서비스
`콩닥_유료서비스_개발_핸드오프.md`를 정독하고 그대로 구현한다. 핵심 재확인:
- **가격 확정:** 건별 **2,900원**(첫 결제 **1,900원**, 계정/기기 최초 1회) · 구독 **9,900원/월**. (문서 내 옛 9,900원 단건 표기가 있으면 무시하고 이 값 사용.)
- **심층 리포트 통합 1종**, 건별=게스트 결제 허용, 구독=로그인 필수, **건별+구독 동시 출시.**
- **어드민 우회:** `isEntitled(session, compatId)` 중앙화, `role==='ADMIN'`이면 결제 없이 전체 열람(서버 판정). 일반 계정은 페이월까지만.
- **PG 추상화(중요):** 결제 로직을 `lib/payments/provider.ts` 인터페이스로 격리하고 `lib/payments/toss.ts` 구현. env `PG_PROVIDER=toss`. **나이스로 바뀔 수 있으니** 라우트/DB/UI는 PG를 모르게. 금액·권한 검증은 어댑터 밖 공통 로직에서.
- 데이터 모델 Order/Unlock/Subscription/DeepReport(additive), 웹훅 멱등, 환불(리포트 열람 전 100%).

---

# B. 마스코트 「두근냥」 적용 (하이브리드 확정)
확정안: **A안 두근냥(코럴 클레이 고양이 + 골드 하트-스파크)** = 메인 로고/파비콘/앱아이콘,
**결과화면 커플 구도 = 두근냥 2마리 + 붉은 실**(B안 아이디어 흡수).
※ 앞서 배포된 `두근이_에셋.zip`(PIL 하트)은 **폐기 — 사용 금지.**

## B-0. 마스코트 이미지 생성 (먼저)
**확정 캐릭터 = 두근냥(고양이) 단일 종.** 이미 생성된 **candidate_A(두근냥)**를 캐릭터 캐논으로 고정.
- ★ **일관성 규칙(필수):** 표정·커플 등 모든 파생 컷은 **candidate_A를 레퍼런스 이미지로 사용(img2img / 동일 시드 / 캐릭터 시트 방식)**하고 "표정·포즈만" 변경. 텍스트로 새로 생성해 얼굴이 바뀌면 안 됨(캐릭터 붕괴 금지). 코럴 #FF5C77 바디·골드 하트-스파크·골드 림라이트·w입·볼터치 그대로 유지.

생성물:
1) **두근냥 기본형**(candidate_A) — 로고/아이콘용, 투명 배경 PNG
2) **표정 6종**(심쿵/불꽃/두근두근/오글/현타/팩폭) — candidate_A 기반, 표정만 교체, 투명 PNG
3) **커플 구도 = 두근냥 고양이 2마리 + 붉은 실**(콩=버림). 코럴 두근냥(=콩이) + 크림 두근냥(=닥이), 둘 사이 빛나는 붉은 실이 가운데 하트 형성. 투명 PNG. (프롬프트는 B-0 하단.)
- 저장: `public/mascot/`(기본형·표정), `public/mascot/couple/`(커플).
- 파비콘/OG/앱아이콘은 두근냥 기본형에서 파생.

**커플(고양이) 프롬프트:**
```
Two cute chibi 3D soft-clay render cat characters facing each other, same round chubby cat
design as the reference (tiny rounded ears with thin gold inner accent, gold rim light,
black dot eyes, tiny w-mouth, rosy blush); left cat matte coral-pink (#FF5C77),
right cat warm cream-gold (#FFF6F1 body, #FFC24B accents); a delicate glowing red thread of
fate connects them and forms a small heart in the middle; soft glossy studio lighting,
dark plum (#2B2430) background, kawaii, ultra cute, romantic. Keep both faces identical to the reference cat.
```

## B-1. 정적 에셋 교체
| 생성물 | 대상 경로 |
|---|---|
| 두근냥 파비콘 | `app/favicon.ico` |
| 두근냥 OG(1200×630, "우리, 얼마나 잘 맞을까?") | `public/og-image.jpg` |
| 앱아이콘 192/512/maskable | `public/icons/icon-192.png` · `icon-512.png` · `icon-512-maskable.png` |
| apple-touch-icon(180) | `public/apple-touch-icon.png` (+ head 링크) |
| 두근냥 기본형 | `public/mascot/doogeunyang_main.png` |
| 표정 6종 | `public/mascot/reactions/*.png` |
| 커플(붉은 실) | `public/mascot/couple/couple_*.png` |
- manifest.json 아이콘 경로/사이즈 확인, 웹푸시 알림 아이콘도 icon-192로.
- 사이트 내 **기존 콩닥이/하트/K-Destiny 파비콘 참조를 두근냥으로 전부 교체**(grep 확인).

## B-2. 결과 화면 커플 구도 (`CompatResultClient.tsx`)
결과 상단(점수 근처)에 **커플 삼각 구도**:
`[왼쪽: 나]  —  [가운데: 두근냥 리액션 or 커플+붉은 실, 점수]  —  [오른쪽: 상대]`
- 가운데 이미지는 **점수→표정 매핑**:
  - 90~100 → 심쿵 / 80~89 → 불꽃 케미 / 70~79 → 두근두근 / 60~69 → 오글 / 60 미만(있다면) → 현타
  - 커플(붉은 실) 버전을 쓰면 점수에 따라 실의 팽팽함·하트 크기·표정 변주.
- 양옆 나/상대는 이름(또는 이니셜) 라벨. 반응형: 모바일 세로 좁으면 구도 유지하되 축소.
- **두근냥 한마디(보이스 카피):** 점수대별 1줄(예: 90+ "이 조합, 나 지금 심장 뛰는 거 안 보여?? 🔥" / 60~69 "티키타카는 원래 시끄러운 거야 ㅋㅋ"). 부정 결과도 재미로 포장(커플 갈등 유발 카피 금지).

## B-3. 페이월과의 결합 (같은 파일, 한 번에)
같은 `CompatResultClient.tsx`에서 커플 구도는 **상단**, A의 페이월 티저는 **하단**. 한 번의 편집으로 둘 다 반영(중복 편집·충돌 금지).
> 🔒 이 커플의 갈등 포인트 3개와 관계 조언이 준비됐어요
> [잠금 해제 · 2,900원(첫 결제 1,900원)]  [무제한 구독 · 9,900원/월]
- 언락/어드민이면 티저 대신 전체 리포트. 프리미엄 공유 카드에도 두근냥 반영.

## B-4. 하지 말 것
- 무료 궁합/공유(K-loop)·엔진·기존 결제/로그인 로직 변경 금지.
- 두근냥 이미지 비율 왜곡 금지, 배경 투명 PNG 유지. 색은 브랜드 팔레트(코럴 #FF5C77 계열) 유지.
- 폐기된 `두근이_에셋.zip`(PIL 하트) 사용 금지.

---

## 완료 보고 (Opus5·Cowork 검수용)
1. 변경/신규 파일 전체 목록(A·B 구분).
2. 테스트 방법: 어드민 우회 확인법 + 토스 테스트 결제(건별/구독) + 결과화면 점수별 두근이 표정 전환 확인.
3. prisma additive 확인, `npm run build` 통과 로그, 커밋 시크릿 0건.
4. 남은 옛 콩닥이/하트/K-Destiny 파비콘 잔재 grep 결과(0이어야 함).
