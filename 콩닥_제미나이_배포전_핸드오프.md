# 콩닥 배포 전 마지막 2건 — 제미나이(안티그래비티) 핸드오프

**모델:** 3.7 Flash(기본). 막히면 3.1 Pro로 승격.
**작업 순서:** 프롬프트 A(약관/개인정보) → 프롬프트 B(마스코트). 각각 끝나면 `npm run build` 통과 확인 후 Cowork(검수) → Opus5(최종).
**절대 규칙:** 시크릿 커밋 금지, en/de/es/fr/ja.json 등 다른 로케일 파일은 손대지 말 것(동면 상태 유지), 기존 스타일·애니메이션 클래스 보존.

---

## 프롬프트 A — 이용약관 · 개인정보처리방침 국문 반영

```
너는 콩닥(kongdak) 프로젝트의 프런트엔드 개발자다. K-Destiny 시절의 옛 약관/개인정보 문구가 그대로 남아 있어, 콩닥용 새 문구로 교체해야 한다.

[콘텐츠 원본 — 이것이 유일한 진실]
저장소 루트의 `콩닥_약관_개인정보_초안.md` 파일(확정본 v3). 여기 들어있는 이용약관(제1~11조 + 부칙)과 개인정보처리방침(1~11항)이 최종 반영할 내용이다. 값(운영사=디아이컴퍼니, 시행일=2026-09-01, 문의=help@kongdak.kr, 개인정보 보호책임자=역할명, 위탁·국외이전 6개 업체)은 모두 확정되어 있으니 **초안 그대로 반영한다.** 없는 값을 임의로 지어내지 마라. 초안 맨 아래 "C. 사업자정보"와 "운영 메모"는 **약관/개인정보 페이지 본문에 넣지 마라**(푸터·별도 처리 대상). 없는 값을 임의로 지어내지 마라.

[반영 대상 파일]
1. `messages/ko.json` 의 "Legal" 섹션 — 화면에 뜨는 실제 텍스트.
2. `app/[locale]/terms/page.tsx` — 이용약관 페이지 골격.
3. `app/[locale]/privacy/page.tsx` — 개인정보처리방침 페이지 골격.

[작업 방법]
1. 먼저 terms/page.tsx, privacy/page.tsx 를 읽어 현재 사용 중인 t("...") 키 구조(terms_s1 ~ , privacy_s1 ~ , 리스트/위탁 행 등)를 파악한다.
2. `콩닥_약관_개인정보_초안.md` 의 조항을 순서대로 각 섹션 키에 매핑해 ko.json "Legal" 값을 새로 쓴다.
   - 이용약관: 제1조=s1 … **제11조=s11** (기존 골격이 s10까지면 terms_s11_title/body 키와 렌더 섹션을 추가). 순서 유지.
   - 개인정보: 1항=s1 … 11항=s11, 순서 유지.
3. **구조가 원본과 맞지 않으면 page.tsx 를 원본에 맞춰 수정하라.** 예: 초안의 위탁·국외이전 표는 **6개 업체 행**(Supabase/Contabo/Cloudflare/Google Gemini/Google Analytics/소셜로그인)이고 각 행에 소재 국가가 있다. 기존 privacy 골격의 행 수가 다르면 page.tsx 의 렌더링 섹션·리스트·행 개수를 초안에 맞게 늘리거나 줄인다. **콘텐츠 충실도가 기존 골격보다 우선하며, 국외이전 문구(이전 항목·국가·목적·보유기간)가 누락되지 않게 하라.**
4. 스타일은 절대 바꾸지 마라. 기존 className, framer-motion 애니메이션, 아이콘, 레이아웃은 그대로 두고 텍스트와 반복 구조 개수만 손댄다.
5. "오락 및 자기이해를 위한 참고 정보" 면책(초안 제3조)은 반드시 독립된 눈에 띄는 섹션으로 유지한다.
6. last_updated / badge 등 메타 문구도 콩닥에 맞게 자연스럽게 정리한다(예: last_updated 는 [시행일] 자리 사용).

[하지 말 것]
- en/de/es/fr/ja.json 등 다른 언어 파일은 건드리지 마라(동면 상태).
- 상호·대표자·사업자등록번호·통신판매업번호는 지금 넣지 마라(결제 붙일 때 추가 예정).
- 개인정보 보호책임자에 실명·전화번호를 넣지 마라. 초안대로 역할명 + help@kongdak.kr 만.

[완료 기준]
- `npm run build` 통과, 타입 에러 0.
- /ko/terms, /ko/privacy 페이지가 초안(확정본 v2) 내용과 1:1로 일치한다.
- 변경 파일 목록과, page.tsx 구조를 바꿨다면 무엇을 왜 바꿨는지 3줄 요약 보고.
```

---

## 프롬프트 B — 마스코트 "콩닥이" 배치

```
너는 콩닥(kongdak) 프로젝트의 프런트엔드 개발자다. 브랜드에 귀여움 포인트가 없어서, 새로 만든 마스코트 "콩닥이"를 앱 곳곳에 자연스럽게 배치한다.

[에셋 — 이미 저장소에 있음]
- public/mascot/kongdak-mascot-256.png  (UI 기본 사용)
- public/mascot/kongdak-mascot-512.png  (고해상도 필요 시)
- public/mascot/kongdak-mascot.svg      (선명한 확대 필요 시)
콩닥이는 코럴색 하트 몸통에 큰 눈·볼터치·반짝이가 있는 캐릭터다.

[1단계 — 공용 컴포넌트 생성]
`components/KongdakMascot.tsx` 를 새로 만든다.
- props: size(px, 기본 96), animate('bounce' | 'heartbeat' | 'none', 기본 'heartbeat'), className.
- next/image 로 kongdak-mascot-256.png 렌더(작은 크기는 256, size>=200 이면 512 사용).
- framer-motion 으로 은은한 애니메이션:
  - heartbeat: scale 1 → 1.06 → 1 무한 반복(약 1.2s, easeInOut) — "두근두근" 느낌.
  - bounce: y 0 → -8 → 0 무한 반복.
- 반드시 `prefers-reduced-motion` 존중: 감소 설정이면 애니메이션 끈다.
- alt="콩닥이", priority 는 히어로에서만 true.

[2단계 — 배치]
1. `components/KongdakHero.tsx` (랜딩 히어로): 헤드라인 근처에 콩닥이 하나 크게(size 120~140, heartbeat). 기존 레이아웃/텍스트는 유지하고 시각적으로만 얹는다.
2. `components/CompatResultClient.tsx`:
   - **AI 리딩 로딩 상태**(궁합 결과 생성 대기 중)에 콩닥이(size 96, bounce)와 "두근두근 궁합 보는 중…" 문구. 이게 가장 중요한 포인트다.
   - 점수 카드 옆 또는 상단에 작은 콩닥이(size 48~56, heartbeat) 장식.
3. `components/CompatNewClient.tsx` (입력 폼): 폼 상단이나 제출 버튼 근처에 작은 콩닥이(size 56~64). 제출(계산) 중이면 bounce 로 살짝 생동감.
4. 에러/404 및 빈 상태가 있으면 콩닥이(size 80)와 짧은 안내 문구로 부드럽게.

[규칙]
- 한 곳에서만 정의한 <KongdakMascot/> 를 재사용한다(중복 구현·중복 이미지 태그 금지).
- 브랜드 컬러(coral #FF5C77, plum #6A2C70, gold #FFC24B, cream #FFF6F1) 유지.
- 이미지 최적화: 반드시 next/image 사용, 적절한 width/height 지정으로 CLS 방지.
- 과하지 않게. 화면당 콩닥이는 1~2개까지. 텍스트 가독성·기존 기능 저해 금지.
- 다른 로케일 파일·결제/매칭 로직은 손대지 마라.

[완료 기준]
- `npm run build` 통과.
- 랜딩/입력/결과(특히 로딩) 화면에 콩닥이가 자연스럽게 보이고, prefers-reduced-motion 에서 정지.
- 변경/생성 파일 목록 보고.
```

---

## 검수 순서 (참고)
1. 위 A, B 완료 → Gemini가 `npm run build` 통과 확인.
2. Cowork(나)에게 diff 전달 → 프리체크.
3. Opus5 최종 검수(`콩닥_Opus5_검수인계.md` 프롬프트) → Go 판정.
4. `scripts/safe_deploy.py` 배포 → https://kongdak.kr 실측(국문 랜딩·궁합 플로우·카톡 카드·소셜 로그인·GA DebugView).
