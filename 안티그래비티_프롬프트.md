# 안티그래비티용 프롬프트 (아래 블록 전체를 복사해서 붙여넣기)

---

## 작업: SEO 색인 수정 검증 → 커밋/푸시 → 배포 → 프로덕션 검증

너는 이 프로젝트(k-destiny, Next.js 16.2.9 + next-intl, Windows 로컬 / VPS 배포)에서
**이미 로컬 파일에 적용 완료된 SEO 수정**을 검증하고 배포하는 역할이다.
코드를 새로 설계하지 말고, 아래 절차를 순서대로 실행하면서 각 단계의 **합격 조건**을 확인해라.

### 배경 (왜 이 수정을 했는가)

Google Search Console에서 "페이지 색인이 생성되지 않는 새로운 이유" 메일이 왔다.
색인 안 됨 48개 / 색인 됨 10개. 사유는 다음과 같다.

- 적절한 표준 태그가 포함된 대체 페이지: 5
- 리디렉션이 포함된 페이지: 1
- 발견됨 - 현재 색인이 생성되지 않음: 39
- 크롤링됨 - 현재 색인이 생성되지 않음: 2
- 중복 페이지, Google에서 사용자와 다른 표준을 선택함: 1

**근본 원인**: `app/[locale]/layout.tsx`가 사이트 전체의 유일한 메타데이터 소스였고,
거기에 `canonical: https://thekdestiny.com/{locale}`이 하드코딩돼 있었다.
Next.js App Router는 메타데이터를 하위 페이지가 상속하는데, 하위 페이지가 전부 `"use client"`라
자체 메타데이터를 내보낼 수 없었다. 그 결과 `/en/pricing`, `/ko/guide` 등 모든 페이지가
"내 표준 URL은 로케일 홈이다"라고 구글에 선언 → 구글이 지시대로 색인에서 제외했다.

부수적으로 hreflang이 전 페이지 공통으로 로케일 홈만 가리켜 상호 참조가 깨져 있었고,
`robots.txt`의 `Disallow: /dashboard/` 규칙이 실제 URL `/en/dashboard`와 매칭되지 않아
인증 페이지가 전부 크롤링되고 있었다.

### 이미 변경된 파일 (13개, 로컬에 저장 완료 — 다시 수정하지 마라)

| 파일 | 상태 | 내용 |
|---|---|---|
| `lib/seo.ts` | 신규 | 라우트별·언어별 메타데이터 중앙 관리. `buildPageMetadata()`, `createPageMetadata()` |
| `app/[locale]/layout.tsx` | 수정 | 로케일 홈('') 메타데이터만 담당, 하드코딩 hreflang `<link>` 제거 |
| `app/[locale]/{pricing,guide,input-destiny,sync,select-master,terms,privacy,login}/layout.tsx` | 신규 8개 | `"use client"` 페이지 옆의 얇은 서버 레이아웃. 자체 canonical/hreflang/title 선언 |
| `app/sitemap.ts` | 수정 | 리디렉션되는 루트 `/`와 noindex인 `/login` 제거, hreflang 클러스터 추가 |
| `app/robots.ts` | 수정 | 로케일 접두사 붙은 Disallow 규칙 생성 |
| `next.config.ts` | 수정 | `sitemap.xml`/`robots.txt`를 `no-store` 대상에서 제외 + 캐시 헤더 추가 |

### 반드시 지킬 규칙 (AGENTS.md)

1. `npm run build`를 절대 `| tail` 등 파이프에 연결하지 마라. 파이프는 종료 코드를 가려서 실패한 빌드를 성공으로 오인하게 만든다. (과거 502 장애 원인)
2. 배포는 오직 `python scripts/safe_deploy.py`로만. 직접 SSH로 pm2를 만지지 마라.
3. 시크릿(`sk-`, `password=`, `DEPLOY_PASS=`, `BEGIN ... PRIVATE KEY`)을 커밋/출력/로그에 절대 넣지 마라.
4. `.env`, `.env.local`, `scripts/deploy.env`는 커밋 대상이 아니다.
5. **각 단계의 합격 조건을 만족하지 못하면 다음 단계로 넘어가지 말고 멈춰서 나에게 보고해라.**

---

## 1단계 — 로컬 빌드

```
npm run build
```

**합격 조건**: 종료 코드 0, `Compiled successfully`.

**실패 시**: 에러 메시지 전문을 나에게 보여주고 **멈춰라. 절대 푸시하지 마라.**
타입 에러가 `@/lib/seo`를 못 찾는다고 나오면 `tsconfig.json`의
`compilerOptions.paths`에 `"@/*": ["./*"]`가 있는지 확인해라 (원래 있다).

## 2단계 — 로컬 렌더링 검증

백그라운드로 서버를 띄우고 실제 HTML을 확인해라.

```
npm run start
```

다른 터미널에서 (PowerShell):

```powershell
curl.exe -s http://localhost:3000/en/pricing | Select-String -Pattern 'canonical|hreflang|<title>'
curl.exe -s http://localhost:3000/ko/guide   | Select-String -Pattern 'canonical|hreflang'
curl.exe -s http://localhost:3000/robots.txt
```

**합격 조건 (4개 전부 만족해야 함)**

1. `/en/pricing`의 canonical이 `https://thekdestiny.com/en/pricing` — **`/en`이 아니어야 한다**
2. `/en/pricing`의 hreflang `ko`가 `https://thekdestiny.com/ko/pricing` — **`/ko`가 아니어야 한다**
3. `/en/pricing`의 `<title>`이 홈 제목("AI-Powered Saju Astrology — Unlock Your Cosmic Blueprint")이 **아니고**, `Pricing — Free and Premium Saju Readings | K-Destiny` 여야 한다
4. `robots.txt`에 `Disallow: /en/dashboard`, `Disallow: /ko/dashboard` 같은 **로케일 접두사 규칙**이 존재

하나라도 틀리면 멈추고 실제 출력값을 나에게 보여줘라.
확인 끝나면 `npm run start` 프로세스를 종료해라.

## 3단계 — 커밋 & 푸시

먼저 스테이징 diff에 시크릿이 없는지 스캔해라.

```
git status
git add lib/seo.ts app/sitemap.ts app/robots.ts next.config.ts "app/[locale]/layout.tsx" "app/[locale]/pricing/layout.tsx" "app/[locale]/guide/layout.tsx" "app/[locale]/input-destiny/layout.tsx" "app/[locale]/sync/layout.tsx" "app/[locale]/select-master/layout.tsx" "app/[locale]/terms/layout.tsx" "app/[locale]/privacy/layout.tsx" "app/[locale]/login/layout.tsx"
git diff --staged --stat
```

`SEO_배포_체크리스트.md`와 `안티그래비티_프롬프트.md`는 커밋하지 마라 (작업 문서일 뿐이다).

```
git commit -m "fix(seo): per-route canonical/hreflang + locale-aware robots

app/[locale]/layout.tsx가 유일한 메타데이터 소스라 모든 하위 페이지가
canonical=/{locale}을 상속했고, GSC가 이를 '적절한 표준 태그가 포함된
대체 페이지'로 분류해 색인에서 제외하고 있었다.

- lib/seo.ts 신설: 라우트별·언어별 메타데이터 중앙 관리
- 라우트별 서버 layout.tsx 8개 추가로 자체 canonical/hreflang/title 선언
- robots.ts: /dashboard/ 규칙이 /en/dashboard와 매칭되지 않던 문제 수정
- sitemap: 리디렉션되는 루트와 noindex인 /login 제거, hreflang 클러스터 추가
- next.config: sitemap.xml/robots.txt에 no-store가 걸리던 문제 수정"

git push origin main
```

**중요**: `scripts/safe_deploy.py`는 서버에서 `git reset --hard origin/main`을 실행한다.
**푸시하지 않고 배포하면 이 수정이 반영되지 않을 뿐 아니라 서버가 구버전으로 되돌아간다.**
푸시 성공을 반드시 확인한 뒤 4단계로 가라.

## 4단계 — 배포

```
python scripts/safe_deploy.py
```

**합격 조건**: 출력 마지막에 `Deploy VERIFIED ✅`.

이번 변경에는 Prisma 스키마 변경이 없으므로 DB 마이그레이션 리스크는 없다.
`[DEPLOY FAILED]`가 뜨면 스크립트가 PM2를 재시작하지 않아 구버전이 그대로 살아 있는 상태다.
그 경우 빌드 에러 로그를 나에게 보여주고 멈춰라.

## 5단계 — 사용자 액션 요청 (네가 직접 할 수 없음)

배포가 성공하면 **나에게 Cloudflare 캐시 퍼지를 요청해라.**

> Cloudflare 대시보드 → Caching → Configuration → Purge Everything

이번엔 특히 중요하다. `sitemap.xml`과 `robots.txt`의 `Cache-Control`이
`no-store` → `public, max-age=3600`으로 바뀌는데, 퍼지하지 않으면 Cloudflare가
옛날 응답을 계속 서빙해서 구글이 새 사이트맵을 보지 못한다.

내가 "퍼지 완료"라고 답하면 6단계로 진행해라.

## 6단계 — 프로덕션 검증

```powershell
curl.exe -s  https://thekdestiny.com/en/pricing | Select-String -Pattern 'canonical'
curl.exe -s  https://thekdestiny.com/ko/guide   | Select-String -Pattern 'canonical'
curl.exe -s  https://thekdestiny.com/ja/sync    | Select-String -Pattern 'canonical'
curl.exe -sI https://thekdestiny.com/sitemap.xml
curl.exe -s  https://thekdestiny.com/robots.txt
curl.exe -s  https://thekdestiny.com/sitemap.xml | Select-String -Pattern '<loc>' | Measure-Object -Line
```

**합격 조건**

1. 세 페이지 모두 canonical이 자기 자신의 전체 URL
2. `sitemap.xml` 응답 헤더가 `cache-control: public, max-age=3600` (`no-store`가 아님)
3. `robots.txt`에 `/en/dashboard` 형태 규칙 존재
4. `sitemap.xml`에 루트 `https://thekdestiny.com/`와 `/login`이 **없음**
5. `<loc>` 개수가 **48개** (8개 라우트 × 6개 언어)

## 7단계 — 결과 보고

아래 형식으로 짧게 보고해라. 추측하지 말고 실제 확인한 것만 써라.

```
빌드:        성공 / 실패
로컬 검증:   canonical OK? hreflang OK? title OK? robots OK?
푸시:        커밋 해시
배포:        Deploy VERIFIED 여부
프로덕션:    합격 조건 5개 중 몇 개 통과 (실패 항목은 실제 출력값 첨부)
남은 작업:   (있다면)
```

**마지막으로 나에게 알려줄 것**: Search Console 작업(사이트맵 재제출, URL 색인 요청,
"수정 확인" 버튼)은 배포와 캐시 퍼지가 끝난 뒤 내가 직접 해야 한다는 점을 리마인드해줘.
배포 전에 유효성 검사를 시작하면 실패로 기록돼 재요청까지 더 오래 걸린다.
