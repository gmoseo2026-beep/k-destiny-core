# 콩닥 유료 서비스 개발 — Gemini 핸드오프 (3.1 Pro 권장 · 배포 전 Opus5 보안검수)

**절대 규칙:** 궁합 엔진(lib/compatibility.ts)·K-loop·무료 궁합 흐름 변경 금지. 무료 점수·키워드·공유는 그대로. 동면 로케일 JSON 금지(ko.json만). prisma는 additive(기존 테이블 파괴 금지). 시크릿(토스 키 등) 커밋 금지 → .env. **결제 금액·권한은 반드시 서버가 결정·검증**(클라이언트 신뢰 금지). 완료 시 `npm run build` 통과.

## 확정 사양
- **심층 리포트(통합 1종):** 관계 성향 심층 해석 · 강점 3 / 주의점 3 · **갈등 포인트 & 대처법** · 실천형 조언 · 이번 달 관계운(간단) · 프리미엄 공유 카드.
- **가격:** 건별 **2,900원**(첫 결제 **1,900원**, 계정/기기 최초 1회) · 구독 **9,900원/월**.
- **건별 = 게스트 결제 허용**(이메일만, 로그인 불필요) → 결제 성공 시 해당 궁합(compatId) 언락, 결과 URL로 재열람. 로그인 시 계정에 소장.
- **구독 = 로그인 필수**, 계정 전체 무제한 언락 + 구독 전용(매일/주간 커플 운세, 프리미엄 카드 무제한).
- **건별 + 구독 동시 출시.**

## ★ 어드민 테스트 우회 (중요)
- **권한 판정 함수 하나로 중앙화**: `isEntitled(session, compatId)` →
  1) `session.user.role === 'ADMIN'` 이면 **무조건 true**(결제 없이 모든 유료 기능·리포트·구독 전용 열람) — 테스트용.
  2) 활성 구독(Subscription active 또는 User.tier PREMIUM 미만료) 이면 true.
  3) 해당 compatId 건별 언락 존재하면 true.
- 일반 계정: 위 2·3 없으면 **페이월/결제 화면까지만** 도달(결제 미완료 = 언락 안 됨). PG 미승인 상태에선 자연히 "결제 전 단계"까지만 가능.
- 어드민 우회는 서버(entitlement API/리포트 API)에서 판정 — 클라이언트 플래그로만 하지 말 것.

## 데이터 모델 (Prisma, additive)
- **Order**: id, userId?(게스트면 null), email?, compatId?(건별), type('single'|'subscription'), amount, status('pending'|'paid'|'canceled'|'refunded'), provider('toss'), tossPaymentKey?, tossOrderId(unique), createdAt
- **Unlock**(건별 권한): id, compatId, orderId, email?, userId?, createdAt (compatId로 언락 조회)
- **Subscription**: id, userId(unique), status('active'|'canceled'|'past_due'), billingKey, currentPeriodEnd, createdAt
- **DeepReport**(리포트 캐시): id, compatId(unique), content(JSON/text), createdAt — 결제 후 1회 생성 저장, 재열람 시 재생성 금지
- 기존 User.tier/premiumEndDate 재사용 가능.

## ★ PG 추상화 (토스 우선 · 나이스 대체 대비) — 중요
PG는 **토스페이먼츠로 신청하되 심사 결과에 따라 나이스페이먼츠로 바뀔 수 있다.** 따라서 결제 로직을 토스에 직접 박지 말고 **한 곳(어댑터)에 격리**한다. 나머지 앱(Order/Unlock/Subscription/페이월/entitlement)은 PG를 몰라야 한다.
- `lib/payments/provider.ts` — 공통 인터페이스 하나:
  - `requestPaymentParams(order)` (클라 결제창 파라미터), `confirmPayment({paymentKey, orderId, amount})` (서버 승인+금액대조), `issueBillingKey(...)`, `chargeBilling(billingKey, order)`, `cancelPayment(paymentKey, reason, amount?)`, `verifyWebhook(req)` (서명/멱등 파싱).
- 구현체: `lib/payments/toss.ts` (지금 구현). 나이스로 갈 때 `lib/payments/nice.ts`만 추가하고 **env 스위치**(`PG_PROVIDER=toss|nice`)로 교체 — 라우트/DB/UI 수정 0.
- **금액·권한 검증은 어댑터 밖(서버 공통 로직)에서** 수행 → PG 바뀌어도 보안 로직 그대로.
- 결제창 SDK만 provider별로 다르게 로드(토스 위젯 vs 나이스 JS). 결과 리다이렉트 → 공통 `/api/payments/confirm`가 `provider.confirmPayment` 호출.

## 결제 연동 (토스페이먼츠 = 1차 구현체)
- env: `PG_PROVIDER=toss`, `NEXT_PUBLIC_TOSS_CLIENT_KEY`, `TOSS_SECRET_KEY` (없으면 **토스 테스트 키**로 개발/테스트, 실키는 PG 승인 후 교체). 나이스 전환 대비 키 네이밍은 provider 접두사 유지(`NICE_*`). 시크릿은 .env.
- **건별(일반결제):** 클라 결제창 요청 → 성공 리다이렉트 → **서버 `/api/payments/confirm`** 이 토스 승인 API 호출(금액을 **서버가 보관한 값**과 대조) → 성공 시 Order paid + Unlock 생성.
  - 첫 결제 1,900원: 해당 계정/기기(이메일)로 최초 1회만 적용, 이후 2,900원. 서버에서 판정.
- **구독(빌링):** 토스 빌링키 발급 → `/api/subscriptions/create` 로 등록 → 매월 자동청구(빌링키로 승인) → currentPeriodEnd 갱신. 해지 시 status canceled, 만료까지 유지.
- **웹훅** `/api/webhooks/toss`: 결제/정기결제 상태 동기화(멱등 처리).
- 카드정보는 토스가 처리, 우리 미저장.

## 심층 리포트 생성
- 무료 요약과 **별개 프롬프트**(갈등 포인트·대처법·강점/주의점·조언·이번달 관계운). 한자·오행·전문용어 노출 금지 규칙 유지. 결제(또는 어드민 접근) 시 생성 → **DeepReport 저장**, 재열람은 저장본 사용.

## 페이월 UI
- 무료 결과 하단: 티저 + 두 CTA
  > 🔒 이 커플의 갈등 포인트 3개와 관계 조언이 준비됐어요
  > [잠금 해제 · 2,900원(첫 결제 1,900원)]  [무제한 구독 · 9,900원/월]
- 언락(또는 어드민)이면 티저 대신 전체 리포트 표시. 콩닥 라이트 브랜드 톤.

## 환불/청약철회
- 디지털: **리포트 생성(열람) 전 100% 환불** 가능, 열람 후 제한(약관 제9조·전자상거래법 고지). 토스 취소 API 연동. 구독은 해지 시 다음 결제부터 중지(잔여기간 이용).

## 약관/개인정보/푸터
- 약관 제9조(청약철회) 확인. 개인정보에 결제 거래정보 항목 추가(ko.json Legal). 위탁·국외이전 표에 **토스페이먼츠(PG) 수탁자 추가**. **푸터 사업자정보에 대표 전화번호** 반영(값은 사용자 제공).

## 개발 순서
1. 데이터 모델 + `isEntitled`(어드민 우회 포함)
2. 심층 리포트 생성/저장 + 페이월 UI(어드민은 바로 전체 열람 → 콘텐츠부터 테스트 가능)
3. 토스 **건별 결제** + 서버 confirm + Unlock
4. 토스 **구독 빌링** + 갱신/해지
5. 웹훅 · 환불 · 약관/개인정보/푸터 마감

## 완료 기준
- 어드민 로그인 → 결제 없이 심층 리포트·구독 기능 전체 열람. 일반 계정 → 페이월/결제 단계까지만(언락 안 됨).
- 토스 테스트 키로 건별·구독 결제 성공 시 언락/구독 활성, 서버 금액 검증 동작, 웹훅 멱등.
- 리포트 1회 생성·저장·재열람. 무료 궁합/공유/K-loop 회귀 없음. additive 마이그레이션. `npm run build` 통과, 시크릿 커밋 0.
- 변경/신규 파일 목록 + 테스트 방법(어드민 우회·토스 테스트 결제) 보고.

## 배포 전
- 결제·권한은 민감 → **Opus5 보안검수**(금액 위변조·권한 우회·웹훅 위조·시크릿 노출) 후 배포. 실 토스 키는 PG 승인 후 .env 교체 + 재배포.
