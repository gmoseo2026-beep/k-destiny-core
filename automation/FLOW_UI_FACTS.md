# Phase 0: Flow UI 실측 검증 (FLOW_UI_FACTS)

**작업자:** Human (스크린샷 기반 작성)
**목적:** 실제 Flow UI를 확인하고 파이프라인의 핵심 전제들을 수치로 확정합니다. 화면 캡처는 `automation/_facts/` 에 저장하세요.

### F1. 프로젝트 진입 경로
- `https://labs.google/flow` 로 접속했을 때 나오는 화면은 대시보드인가 편집기인가?
  - 답변: 대시보드 후 프로젝트 진입 (편집기)
- 편집기(생성 가능 화면)의 **URL 패턴**은? (예: `.../flow/project/<id>`)
  - 답변: `labs.google/fx/ko/tools/flow/project/<uuid>`
- 캡처 파일명: `screenshot_1.png`

### F2. 모델 선택 UI
- 모델 드롭다운을 여는 요소의 정확한 텍스트/aria-label: 
  - 답변: 프롬프트 입력창 안의 '설정' 아이콘 클릭 -> 에이전트 설정 패널
- 목록에 실제로 표시되는 항목 전체: 
  - 답변: Omni Flash, Veo 3.1 - Lite, Veo 3.1 - Fast, Veo 3.1 - Quality, Veo 3.1 - Lite [Lower Priority]
- 각 항목 옆에 표시되는 **크레딧 숫자**: 
  - 답변: **(중요) 크레딧 숫자가 표시되지 않음!** 오직 모델명만 표시됨.
- **현재 선택된 모델이 UI에서 어떻게 표시되는지**: 
  - 답변: 설정 패널 내의 드롭다운 텍스트로 표시됨. (예: `Veo 3.1 - Fast`)
- 캡처 파일명: `screenshot_3.png`

### F3. 출력 개수(Outputs per prompt) 설정
- 설정 위치와 현재 값: 
  - 답변: 에이전트 설정 패널 내 '동영상 생성 기본값' 하단. 현재 값 `1x`
- 1로 설정 가능한지, 기본값이 몇인지: 
  - 답변: `1x`, `x2`, `x3`, `x4` 중 선택 가능하며 `1x` 로 설정 완료.
- 캡처 파일명: `screenshot_3.png`

### F4. 크레딧 잔액 표시 ★ 가장 중요
- 잔액이 화면 어디에 어떤 형식으로 뜨는가? (예: `9,840 credits`)
  - 답변: 우상단 프로필 메뉴 클릭 시 내부에 `XXXXX Google Flow credits` 형식으로 표시. (영어 UI 기준)
- **1회 생성 전/후 잔액 차이 (실단가 확인)**:
  - 생성 전 잔액: `25010`
  - 생성 후 잔액: `25000` (Veo 3.1 - Lite 1회 생성)
  - 차이(= 실단가): **정확히 `10` 크레딧 차감됨.**
- **Extend 1회 시 차이**:
  - 차이(= Extend 1회 실단가): Extend 기능 미지원으로 확인 안 됨.
- 캡처 파일명: `screenshot_2.png`, `screenshot_5.png`, `screenshot_7.png`

### F5. Extend 다운로드 의미 ★★ 가장 중요 (파이프라인 근본 전제)
- 8초 클립 1개 생성 후, Extend 1회 수행한 뒤 다운로드한 mp4의 재생 길이(ffprobe)를 기록하세요.
  - 초기 클립 길이: **정확히 `8초`**
  - Extend 후 다운로드 영상 길이: 테스트 불가
- 판정:
  - **(중요) 영상의 점 3개 메뉴에 'Extend' (연장) 기능이 없음.** 대신 `Add to scene` 이라는 기능이 존재함 (장면 빌더로 넘기는 기능으로 추정).
- (선택) Extend 2회까지 더 해보고 길이 변화 기록: 
  - 해당 없음
- 캡처/영상 파일명: `screenshot_8.png`

### F6. 결과 타일 구조
- 생성이 끝나면 결과가 어디에 어떤 DOM 구조로 쌓이는가? (타임라인? 갤러리? 리스트?)
  - 답변: 메인 화면 갤러리(그리드) 형태.
- 각 결과에 **고유 식별자(data 속성, id, href, blob URL)**가 있는가?
  - 답변: DOM을 보지 못했으나 타일 형태로 구성됨.
- 다운로드 버튼이 **결과 타일 안에** 있는가, 전역 툴바에 있는가?
  - 답변: 각 영상 타일의 '점 3개' 메뉴 내에 '다운로드' 버튼 존재.
- **(중요) 결과 추가 순서:** 최신 결과물이 **왼쪽 (앞쪽)** 에 추가됨 (prepend 방식).
- 캡처 파일명: `screenshot_5.png`

### F7. 생성 중 / 큐 상태 표시
- "대기 중 / 생성 중 / 완료" 각 상태의 화면 문구를 **한글·영문 그대로** 적기
  - 답변: 우하단 토스트 알림으로 표시됨. "영상이 예약되어 대기열에서 기다리고 있습니다. 완성될 때까지 잠시만 기다려 주세요!"
- Lower Priority 큐 대기 시 특별한 문구가 있는가?
  - 답변: 확인 안 됨
- 실제 대기 시간 실측: 
  - 초기 생성: 확인 안 됨
  - Extend: 확인 안 됨
- 캡처 파일명: `screenshot_5.png`

### F8. 에러 표시
- 생성 실패 시 뜨는 요소의 구조 (토스트? 인라인?) 와 `role` 속성:
  - 답변: 우측 에이전트 채팅 패널 내의 AI 응답 메시지로 표시됨.
- 정확한 문구: 
  - 답변: "죄송합니다. 현재 설정된 비디오 모델을 사용할 수 없는 상태입니다. 대신 Omni Flash 또는 Veo 3.1 - Lite 모델을 사용하여 영상을 생성해 드릴 수 있습니다. 어떤 모델로 진행할까요?" (Lite Lower Priority 선택 시 발생)
- 캡처 파일명: `screenshot_6.png`

### F9. 다운로드 방식
- 다운로드 클릭 시 브라우저 다운로드 이벤트가 발생하는가, 새 탭에서 열리는가?
  - 답변: 새 탭이 열리지 않고 **현재 창에서 브라우저 다운로드 이벤트 발생.**
- 파일명 패턴: 
  - 답변: `{프롬프트},_8초_{화질}p_{날짜시간}.mp4` (예: `멋진촛불,_8초_1080p_202607260806.mp4`)
- 화질 선택(720p/1080p) 팝업이 뜨는가? 
  - 답변: **예 (1080p까지 무료로 다운 가능 안내 모달 창 뜸)**
- 기타 메뉴 (점 3개): Favorite, Reuse prompt, Add to scene, Add to prompt, Download, Rename, Share, Publish to YouTube, Set project cover, Flag output, Move to trash
- 캡처 파일명: `screenshot_8.png`

---
**검토 완료 시:** 이 파일의 빈칸을 모두 채운 후 에이전트에게 "Phase 0 완료"를 알려주세요.
