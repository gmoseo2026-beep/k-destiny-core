# 콩닥 개발 핸드오프 — 제미나이(안티그래비티) 실행 가이드

**워크플로우:** 제미나이(안티그래비티)가 개발 → 터미널에서 **Opus5(Claude Code)**가 최종 검수.
**같이 쓰는 문서:** `AGENTS.md`(규칙), `콩닥_PhaseA_개발스펙.md`(무엇을 만들지), `콩닥_브랜드보드.html`(색·로고).

---

## 1. 셋업 (한 번만)

1. 저장소 루트에 **`AGENTS.md`** 를 넣는다(제공함). 안티그래비티가 자동으로 읽는다.
2. 강제 적용을 원하면 같은 내용을 **`.agents/rules/kongdak.md`** 로도 저장하고 활성화 옵션을 **Always On**으로 둔다. (규칙당 최대 12,000자)
3. `콩닥_PhaseA_개발스펙.md`, `콩닥_브랜드보드.html`을 저장소에 두어 `@파일명`으로 참조 가능하게 한다.
4. `.env`에 키 준비: `GEMINI_API_KEY`(과금 프로젝트), `DATABASE_URL`(Supabase), 토스/카카오 결제 키. **키는 절대 커밋 금지.**

---

## 2. 어떤 모델에게 시킬까 — 3.1 Pro vs 3.7 Flash

**결론: Flash를 기본 일꾼으로, Pro는 설계·난제 전용.** (최신 벤치마크상 3.x Flash가 코딩·롱컨텍스트에서 Pro와 대등하거나 우세하고, 더 빠르며 수정 루프가 짧음. 울트라 무제한급이라 비용은 고려 안 해도 됨 → **품질·속도 기준**으로 배분.)

| 작업 | 모델 |
|---|---|
| **전체 아키텍처·단계 설계**, DB 스키마 설계 | **3.1 Pro** (1회 계획) |
| **궁합 점수 알고리즘 설계**(5축 산식·가중치 결정) | **3.1 Pro** |
| 실제 구현·파일 편집·컴포넌트/화면 | **3.7 Flash** (기본) |
| 테스트 작성, 리팩터, 린트/빌드 루프, 터미널 작업 | **3.7 Flash** |
| OG 공유카드, 결제 연동 등 잘 정의된 작업 | **3.7 Flash** |
| **Flash가 같은 오류로 2회+ 막힌 디버깅** | **3.1 Pro로 에스컬레이션** |
| 최종 검수 | **Opus5(터미널)** — 항상 |

**운영 원칙:** ① Pro로 계획 한 번 → ② Flash로 구현 루프를 빠르게 → ③ 막히면 Pro → ④ 기능 완료 시 Opus5 검수. 애매하면 같은 태스크를 두 모델에 시켜 결과를 비교(스펙에도 권장).

---

## 3. 실행 순서 (스펙 §9와 동일)

1. 측정·법무 기반(GA4 이벤트6·푸터 사업자/약관/환불) → 2. 결제(토스/카카오 단건) → 3. **궁합 엔진** → 4. AI 해석 → 5. 화면 플로우 → 6. 공유카드/OG → 7. 리브랜딩 → 8. 배포.

> 추천 시작점: **3번 궁합 엔진**(서비스의 심장, 바로 테스트 가능). 아래 프롬프트 B로 시작.

---

## 4. 붙여넣는 프롬프트 (그대로 복사)

### 프롬프트 A — 최초 계획 (모델: 3.1 Pro)
```
너는 콩닥(kongdak) 프로젝트의 개발 에이전트다. 먼저 저장소 루트의 @AGENTS.md 와 @콩닥_PhaseA_개발스펙.md 를 정독해라.
지금은 코드를 작성하지 말고, Phase A를 실행 순서(§9)대로 쪼갠 구현 계획만 제시해라:
- 각 단계별로: 만들/수정할 파일, 재사용할 기존 파일, 완료 정의, 예상 리스크.
- 특히 lib/compatibility.ts(궁합 점수 5축 산식·가중치)와 Prisma Compatibility 모델 설계를 구체적으로.
- 범위 밖(매칭·구독·본인인증)은 절대 포함하지 마라.
계획을 내가 승인하면 단계별로 진행한다.
```

### 프롬프트 B — 궁합 엔진 구현 (모델: 3.7 Flash, 산식이 애매하면 3.1 Pro)
```
@AGENTS.md 와 @콩닥_PhaseA_개발스펙.md §3(궁합 알고리즘)을 따른다.
lib/compatibility.ts 를 신규로 구현해라. 요구사항:
- 입력: 두 사람의 SajuResult(기존 lib/saju.ts calculateFourPillars 재사용, 절대 수정 금지).
- 출력: { score(0-100, 60~99 매핑), keywords[3], breakdown(5축 원점수) }.
- 5축 가중: 일간관계30 / 오행상보30 / 지지관계20 / 오행균형10 / 음양조화10 (스펙대로).
- 결정론: 같은 입력이면 항상 같은 결과. AI 호출 금지(문장 해석은 별도).
- 단위 테스트(vitest/jest) 필수: 동일입력=동일점수, 상생/상극/합충 케이스, 경계값.
완료 후: typecheck·lint·build·test 통과 결과와 변경 요약을 보고해라. 범위 밖은 만들지 마라.
```

### 프롬프트 C — 기능 완료 → Opus5 검수 인계문 생성 (Flash)
```
방금 작업을 Opus5 검수용으로 정리해라(코드 수정 금지):
1) 변경/추가 파일 목록과 목적 2) 궁합 점수 산식 요약 3) typecheck/lint/build/test 결과
4) 보안·PII·결제 관련 변경점 5) 내가 스스로 의심스러운 부분·미확인 가정.
이 요약을 REVIEW_HANDOFF.md 로 저장해라.
```

### 터미널에서 Opus5(Claude Code)에게 — 최종 검수
```
콩닥 Phase A의 최근 변경을 검수해줘. 기준: 저장소 루트 AGENTS.md의 절대규칙(범위 고정, 엔진 보존,
결정론+AI 분리, 한자·전문용어 금지, 결제 포인트충전 금지, PII 보호, additive 마이그레이션)과
콩닥_PhaseA_개발스펙.md. REVIEW_HANDOFF.md를 참고하되 곧이곧대로 믿지 말고 실제 코드로 검증해라.
특히 lib/compatibility.ts의 결정론성·경계값, 결제 키 하드코딩 여부, PII 노출, 측정 이벤트 발사,
스펙 범위 이탈(매칭·구독 등)을 중점으로 지적하고 수정안을 제시해줘.
```

---

## 5. 자주 하는 실수 방지(제미나이용 리마인더)
- 범위 확장 금지(매칭·구독·본인인증 X). 요구 안 한 리팩터 금지.
- `lib/saju.ts` 수정 금지. 궁합은 별도 모듈.
- 점수를 AI로 계산하지 말 것(결정론 유지).
- 한자/전문용어 UI 노출 금지.
- 키 하드코딩·`.env` 커밋 금지. 마이그레이션은 파괴 없이 추가만.
- 같은 오류 2회 실패 시 멈추고 요약 보고(→ Pro 또는 사용자).
