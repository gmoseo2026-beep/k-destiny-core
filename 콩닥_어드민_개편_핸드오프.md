# 콩닥 어드민 개편 — 운영·CS 실전용 (Gemini 핸드오프)

**목표:** `/ko/admin`을 K-Destiny 구독형 회원관리 화면에서 → **콩닥(무료 궁합 + 단건/패스 결제 + 게스트 결제) 운영·고객대응 어드민**으로 개편. 핵심은 "결제 조회·환불·언락 재발급"으로 CS를 클릭으로 처리하는 것.

## 절대 규칙 (보안·안정)
- **모든 어드민 페이지·API는 서버에서 `session.user.role === 'ADMIN'` 검증**(클라 플래그 금지, 비인가 403/리다이렉트).
- **민감 액션(환불·언락 부여/연장·등급 변경)은 서버 API에서만** 수행하고 **감사로그** 기록.
- 궁합 엔진·K-loop·결제 코어(order/complete/webhook/grant/entitlement) **로직 변경 금지**(읽기·호출만). Prisma는 additive.
- 결제 취소는 기존 `revokePaidOrder()` + PortOne `cancelPayment()`(이미 구현됨) 재사용.

## 1. 대시보드 지표 (콩닥 맞춤으로 교체)
현재 "전체 회원 / 오늘의 궁합 / 누적 궁합"에 아래를 추가·정리:
- **매출:** 오늘·이번주·이번달 매출(합계), 결제 건수, 상품별 분포(단건/1개월/3개월), **환불 건수·금액**.
- **전환:** 무료 궁합수 대비 결제수 = 전환율(%), ARPPU, 첫결제(1,900) vs 재결제(2,900) 비율.
- **바이럴/재방문:** 누적·오늘 궁합수(유지), 웹푸시 구독수, (가능하면) ref 유입수.
- **회원:** 전체·신규(오늘/이번주), **활성 이용권 보유수**(premiumEndDate > now).
- K-Destiny 잔재(구독 "등급" 중심 표현) 제거.

## 2. 주문·결제 관리 탭 ★신규 (CS 1순위)
- **Order 목록 테이블:** 결제일 · 주문번호 · 상품(단건/1개월/3개월) · 금액 · **상태(PAID/PENDING/CANCELED)** · 이메일 · 회원/게스트 · compatId(결과 링크) · 결제수단.
- **필터/검색:** 상태, 상품유형, 기간(날짜 범위), 검색(이메일·주문번호·compatId).
- **행 상세:** 해당 주문의 Unlock(만료일 포함)·연결된 compat 링크·PortOne 거래키.
- **★환불 버튼:** PAID 주문 → 확인 모달(사유 입력) → 서버 API가 PortOne 취소 + `revokePaidOrder`(Order CANCELED, Unlock 회수, 패스면 기간 롤백) → 감사로그. (전액/부분 옵션.)
- 오래된 PENDING 주문(미완료) 별도 표시 + 일괄 정리(선택).

## 3. 고객(이메일) 조회 탭 ★신규 (CS 2순위)
- **이메일로 검색** → 그 고객의 **모든 주문 · 언락 · 이용권 상태 · 만료일 · 열람 가능한 리포트**를 한 화면에.
- **"결제했는데 안 열려요" 대응:** 해당 compat 언락 상태 확인 + **언락 수동 재발급 / 만료 연장 버튼**(서버 API, 감사로그). 게스트가 링크를 잃은 경우 결과 링크 재안내.
- 회원이면 계정 정보(가입경로·role·premiumEndDate)도 함께.

## 4. 회원 관리 (기존 테이블 개편)
- 컬럼을 콩닥 맞춤으로: 이름/이메일 · **가입경로**(구글/네이버/카카오/이메일/게스트) · role · **이용권 상태**(없음 / 1개월·3개월 + 만료일 — premiumEndDate 기준) · **누적 결제액** · 최근 활동 · 가입일.
- "무료/프리미엄 등급" 같은 구독 표현 → **이용권 기준**으로 교체.
- 어드민 승급/강등(테스트용, 서버 검증 + 감사로그). 카르마/토큰 조정은 콩닥에 불필요하면 제거(사주 챗 잔재면 정리).

## 5. CS 편의
- CSV 내보내기(주문/회원 각각) 유지·개선(필터 반영).
- (선택) 주문·고객 메모 필드.

## 6. 감사로그 (additive)
- `AdminAuditLog` 모델: `id, adminUserId, action, targetType, targetId, detail(JSON), createdAt`.
- 환불·언락 재발급/연장·등급 변경 시 기록. 어드민 화면에 최근 로그 뷰.

## 우선순위 (과도하게 키우지 말 것)
1. **주문·결제 관리 + 환불**(§2)
2. **이메일 고객 조회 + 언락 재발급**(§3)
3. **대시보드 매출 지표**(§1)
4. 회원관리 컬럼 개편(§4) → 그다음.

## 완료·검수
- `npm run build` 통과, additive 마이그레이션, 시크릿 커밋 0.
- 변경/신규 파일 목록 보고.
- **환불·언락 재발급 API는 민감**하므로 배포 전 Opus5 보안검수(권한우회·IDOR·환불 위변조) 대상에 포함.
