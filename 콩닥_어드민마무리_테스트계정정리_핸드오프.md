# 콩닥 어드민 잔재 마무리 + 테스트 계정 정리 — Gemini 핸드오프 (3.1 Pro 권장)

**절대 규칙:** 궁합 엔진·결제·K-loop·로그인 로직 손대지 말 것. 동면 로케일 JSON 금지(ko.json만). 완료 시 `npm run build` 통과. **DB 삭제는 반드시 dry-run 먼저 → 사용자 확인 후 실삭제.**

---

## 1. 어드민 남은 잔재 (코드)
관리자 화면(app/[locale]/admin/**)에 남은 K-Destiny식 요소를 콩닥 Phase A에 맞게 정리:
- **"Exit Admin" → "관리자 나가기"** (영문 제거).
- 지표(카드) 콩닥화:
  - **"오늘의 사주" → "오늘의 궁합"** (콩닥은 궁합 서비스. 오늘 생성된 Compatibility 수).
  - **"유료 구독자", "활성 구독"** 등 구독/결제 지표는 **아직 결제 미도입이라 제거하거나 숨김**(결제 붙는 Phase A.5에 되살림). 대신 콩닥에 맞는 지표로 교체 권장: 전체 회원 / 오늘의 궁합 / 누적 궁합 수 / 공유(share_created) 수 등 실제 존재하는 데이터 기준.
  - 남은 영어 문구(있으면) 한국어로.
- 상단 "ADMIN" 배지 등 표기는 한국어("관리자")로 통일해도 좋음(선택).

## 2. 테스트 계정 정리 (DB, 런칭 전 클린 슬레이트)
현재 회원 17명은 전부 개발자 본인의 **테스트 계정**(네이버/카카오/구글 반복 로그인으로 생성). 런칭 전 깨끗이 비운다.

- `scripts/cleanup_test_users.ts` (또는 유사) 스크립트를 작성:
  1. 먼저 **prisma/schema.prisma를 읽어** User 와 연결된 테이블(Account, Session, Compatibility 등)의 관계·삭제 순서를 파악.
  2. **보존 대상**: 관리자 계정(role === 'ADMIN', 즉 수경 계정). 이것만 남긴다.
  3. **삭제 대상**: 그 외 모든 User + 그들에 연결된 Account/Session/(있으면)Compatibility 등 관련 행을 FK 순서에 맞게 삭제.
  4. **반드시 2단계 실행**: 기본은 **dry-run**(삭제될 대상 수·이메일만 출력, 실제 삭제 X). 환경변수/인자(예: `--confirm`)를 줬을 때만 실제 삭제.
- 스크립트 상단에 사용법 주석: dry-run 실행법, 실삭제 실행법.
- ⚠️ 궁합 엔진·결제 로직은 건드리지 말 것. 데이터만.

## 3. (참고, 코드 아님) 향후 같은 사람 중복가입 방지
- 같은 사람이 여러 소셜로 각각 계정이 생기는 건, 이메일 기반 계정 연동을 켜야 합쳐진다. 다만 보안 주의(이메일 선점 위험)라 **지금은 하지 말고 Phase B 과제로** 메모만. (실사용자는 대부분 한 소셜만 쓰므로 실무 영향 작음.)

## 완료 기준
- 어드민: "Exit Admin"·"오늘의 사주"·구독 지표 등 K-Destiny식 잔재 0, 콩닥 Phase A 지표로 표시, 영문 0.
- 정리 스크립트: dry-run으로 삭제 대상(관리자 제외 전원)이 맞게 잡히는지 출력. 사용자가 확인 후 `--confirm`으로 실삭제하면 관리자 1명만 남음.
- `npm run build` 통과. 변경/스크립트 사용법 요약 보고.
