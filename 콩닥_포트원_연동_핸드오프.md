# 콩닥 결제 연동 — PortOne(포트원) v2 전환 핸드오프

**배경:** PG를 토스 직연동 → **PortOne v2 애그리게이터**로 전환. 하위 PG는 **KG이니시스 / NHN KCP** 심사 중.
**핵심 이점:** 포트원은 애그리게이터라 **이니시스·KCP 코드가 동일**하다. 승인된 PG를 **콘솔에서 channelKey만 바꿔** 연결 → 코드 포크 없음. 우리 PG 추상화(lib/payments/provider.ts)에 `portone.ts` 하나만 추가하면 됨.
**지금 상태:** 실키·채널은 심사 후. 지금은 **포트원 테스트 채널/테스트 모드**로 개발·검증하고, 승인 후 채널키·시크릿만 교체.

**바뀌지 않는 것(재사용):** Order 생성 로직, 서버 금액결정, `isEntitled`(패스 만료 판정), 단건/패스 상품·가격, 약관. → PG 호출부만 교체.

---

## 1. 상품/결제수단 범위
- 상품: 단건 2,900(첫1,900) · 1개월 9,900 · 3개월 24,900. **전부 일반결제(1회성)** — 빌링키/정기결제 사용 안 함(구독 폐지 유지).
- payMethod: **초기엔 `CARD` + `EASY_PAY`(간편결제)만.** VIRTUAL_ACCOUNT(가상계좌)·현금영수증·에스크로는 계약/복잡도 이슈라 후순위(디지털 콘텐츠라 에스크로 불요).

## 2. env (시크릿은 .env, 커밋 금지)
```
PG_PROVIDER=portone
NEXT_PUBLIC_PORTONE_STORE_ID=store-...        # 포트원 상점 ID
NEXT_PUBLIC_PORTONE_CHANNEL_KEY=channel-...   # 승인된 PG 채널키(이니시스 or KCP). 테스트는 테스트 채널키.
PORTONE_API_SECRET=...                         # 서버 결제조회/취소용 V2 API secret
PORTONE_WEBHOOK_SECRET=...                      # 웹훅 서명검증용
```
- 이니시스/KCP 둘 다 승인되면 채널키를 둘 다 등록해두고 프론트에서 선택(또는 기본 하나) 가능. 코드는 동일.

## 3. 어댑터: lib/payments/portone.ts (provider 인터페이스 구현)
- `getPayment(paymentId)` → `GET https://api.portone.io/payments/{paymentId}`, 헤더 `Authorization: PortOne <PORTONE_API_SECRET>`. 응답의 `status`, `amount.total` 사용.
- `confirmPayment({paymentId, amount})` → 포트원은 PG에서 승인 완료되므로 **별도 capture 불필요**. getPayment 호출해 `status === 'PAID' && amount.total === order.amount` 확인만. (토스식 confirm 호출 없음.)
- `cancelPayment(paymentId, reason, amount?)` → `POST https://api.portone.io/payments/{paymentId}/cancel` (body: reason, [amount]).
- `verifyWebhook(req)` → `@portone/server-sdk`의 `Webhook.verify(PORTONE_WEBHOOK_SECRET, rawBody, headers)`.
- issueBillingKey/chargeBilling → **미사용**(구독 폐지). 인터페이스 만족용으로 두되 호출 경로 없음/throw.
- `PG_PROVIDER=portone`이면 payments/index.ts가 portone 어댑터를 반환하도록. (toss.ts는 남겨두되 미사용.)

## 4. 주문 생성 (기존 order/route.ts 거의 유지)
- Order.orderId를 포트원 **paymentId로 사용.** 현재 형식 `kd_ord_<uuid32>` = 39자, ASCII → **KCP 40자·ASCII 제한 통과**(1자 여유). orderName은 100바이트 이내(현재 "콩닥 심층 궁합 리포트" OK).
- ★ **paymentId는 시도마다 유일해야 함.** 결제 실패 후 재시도 시 **같은 orderId 재사용 금지** → 재시도는 **새 Order(새 orderId) 생성** 후 재요청. (프론트 재시도 = order 재생성.)
- Order.provider 기본값 "toss" → "portone"로. tossPaymentKey 컬럼은 포트원 transactionId 저장용으로 재사용(또는 컬럼명 유지, 의미만 변경).

## 5. 프론트 결제창 (components — 결과화면 페이월 CTA)
- `npm i @portone/browser-sdk`. 
```ts
import * as PortOne from "@portone/browser-sdk/v2";
const res = await PortOne.requestPayment({
  storeId: process.env.NEXT_PUBLIC_PORTONE_STORE_ID!,
  channelKey: process.env.NEXT_PUBLIC_PORTONE_CHANNEL_KEY!,
  paymentId: order.orderId,               // 서버가 만든 주문번호
  orderName: order.type === 'SINGLE' ? "콩닥 심층 궁합 리포트" : "콩닥 플러스 이용권",
  totalAmount: order.amount,              // 서버 결정 금액
  currency: "CURRENCY_KRW",
  payMethod: "CARD",                      // 또는 "EASY_PAY"
  customer: { fullName, email, phoneNumber },
  redirectUrl: `${origin}/ko/pay/complete?orderId=${order.orderId}`, // 모바일 리다이렉트 복귀
});
// PC: res.code == null 이면 성공(res.paymentId). 오류면 res.code/res.message.
```
- **customer 필수값:** 이니시스/KCP **PC 카드결제는 fullName·phoneNumber·email 필수.** → 결제 전 이름·휴대폰·이메일 입력받기(게스트 단건도 이메일만 말고 **이름·휴대폰 추가 수집**). 안 넣으면 PG창에서 막히거나 반려 사유.
- **모바일**은 redirectUrl로 복귀 → `/ko/pay/complete`에서 orderId로 서버검증 호출. **PC/모바일 둘 다** 최종 판정은 서버검증으로 단일화.

## 6. 서버 검증 (기존 confirm → complete로 개념 변경)
- 엔드포인트 `app/api/payments/complete`(또는 기존 confirm 재활용): body의 paymentId(=orderId) 받아 **서버가 getPayment 호출** → `status==='PAID'` && `amount.total === order.amount`(서버 보관값) 확인 → Order PAID + (SINGLE)Unlock 생성 / (PERIOD_PASS)premiumEndDate 연장. **멱등**(이미 PAID면 스킵).
- 금액·상태 판정은 **서버가 포트원 조회값으로만** — 클라 신뢰 금지(기존 원칙 유지).

## 7. 웹훅 (webhooks/toss → webhooks/portone)
- `app/api/webhooks/portone`: **raw body 필요**(Next route에서 `req.text()`로 원문 확보) → `@portone/server-sdk` `Webhook.verify(PORTONE_WEBHOOK_SECRET, rawBody, headers)` 서명검증 → `type`(`Transaction.Paid`/`Transaction.Cancelled`) 분기 → `data.paymentId`로 **getPayment 재조회**해 상태·금액 교차검증 → DB 동기화(멱등). 위조 방지 동일 원칙.
- 포트원 콘솔에 웹훅 URL 등록(결제모듈 V2, Content-Type application/json). 기존 토스 웹훅 라우트는 제거/비활성.

## 8. 테스트 (실키 전)
- 포트원 **테스트 채널키 + 테스트 API secret**로 전체 플로우 검증(테스트 카드). 어드민 우회로 콘텐츠/페이월 별도 검증.
- 승인 후: 콘솔에서 실채널키 발급 → env 교체 + 재배포. 실결제 1건 e2e(단건 게스트 → 리포트 열람).

## 9. 약관/개인정보 반영
- 개인정보 수탁자에 **㈜포트원(결제대행)** 추가(+실제 하위 PG: KG이니시스/NHN KCP 명시). 현재 "토스페이먼츠"만 있는 수탁자 문구 교체.
- 결제 시 수집하는 **휴대폰번호**를 개인정보 수집항목에 반영(현재 없음).

## 10. 개발 순서
1. `portone.ts` 어댑터 + `PG_PROVIDER=portone` 스위치 + env 세팅(테스트)
2. order 재시도 시 새 orderId 보장 + provider "portone"
3. 프론트 requestPayment + customer(이름·휴대폰·이메일) 입력 + `/pay/complete` 복귀 페이지
4. 서버 complete(검증) + Unlock/패스 연장
5. webhooks/portone(서명검증·재조회·멱등), 토스 웹훅 제거
6. 약관·수탁자·수집항목 반영
7. `npm run build` 통과 + 변경 파일 보고 → Cowork 프리체크 → Opus5 보안검수(금액/서명/멱등) → 배포(테스트)

## 완료 기준
- 테스트 채널로 단건·1·3개월 패스 결제 성공 → 서버검증으로 언락/패스 연장, 금액 서버검증, 웹훅 서명검증·멱등 동작. 어드민 우회 유지. build 통과, 시크릿 커밋 0. 승인 후 실채널키만 교체하면 오픈.
