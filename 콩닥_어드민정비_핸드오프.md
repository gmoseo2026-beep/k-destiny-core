# 콩닥 어드민·네비 정비 — Gemini 핸드오프 (3.1 Pro 권장)

**모델:** 3.1 Pro (중복 회원 원인 조사 + 레이아웃 판단이 섞여 Flash 비권장).
**절대 규칙:** 궁합 엔진(lib/compatibility.ts)·결제·K-loop 손대지 말 것. 동면 로케일 JSON(en/ja/de/es/fr) 텍스트 수정 금지(ko.json만). 로그인/AuthModal 회귀 금지. 완료 시 `npm run build` 통과.

관리자 화면에서 관찰된 실제 문제(스크린샷 기준):
- 상단 네비에 관리자 버튼이 3개 겹침("Admin" 방패 + "ADMIN" 아웃라인 + 주황 "수경…") → 폭 초과로 상단이 잘림(관리자 계정만).
- 어드민 페이지가 옛 K-Destiny: 영문 "GLOBAL COMMAND CENTER", "Monitor system metrics… Kongdak platform.", "Export CSV", "Total Users", "Premium Subscribers" 등 + 다크 테마.
- 회원 목록에 동일 사용자("서민오")가 여러 번 중복 표시. Total Users 14인데 같은 사람이 반복됨.

---

## 1. 네비 관리자 버튼 중복·상단 짤림 (components/Navbar.tsx)
- 관리자에게 노출되는 **관리자 진입 버튼을 하나로 통합**한다. 현재 대시보드 링크 + 관리자 링크 + 로그인/아바타 영역이 겹쳐 3개처럼 보이고 폭을 넘긴다. **"관리자" 버튼 1개만** 남기고 중복 제거.
- 네비가 **어떤 계정(특히 관리자)에서도 상단이 잘리지 않게** 반응형 처리: 모바일에서 항목이 넘치면 축약(아이콘화)하거나 가로 스크롤/줄바꿈. 고정 헤더 높이와 콘텐츠가 겹치지 않도록.
- 라벨은 한국어 "관리자"로 통일(영문 ADMIN 중복 표기 제거).

## 2. 어드민 페이지 콩닥화 (app/[locale]/admin/**)
- 영문·K-Destiny 문구를 **한국어 콩닥 톤**으로 전면 교체:
  - "GLOBAL COMMAND CENTER" → "콩닥 관리자" (또는 "관리자 대시보드")
  - "Monitor system metrics and manage user access across the Kongdak platform." → "콩닥 서비스 지표와 회원을 관리합니다."
  - "Total Users" → "전체 회원", "Premium Subscribers" → "유료 구독자", "Export CSV" → "CSV 내보내기", "Exit Admin" → "관리자 나가기", "USER" 배지 등도 한국어/콩닥 톤.
- 테마를 **콩닥 라이트 브랜드**(cream #FFF6F1 배경, ink #2B2430 본문, coral #FF5C77 포인트)에 맞춘다. 다크로 유지해야 할 이유가 없으면 라이트로. (관리자 전용이라 우선순위는 낮지만 "어디서도 K-Destiny 느낌 없게"가 목표)

## 3. 회원 목록 중복 (원인 조사 후 수정)
동일 사용자가 여러 번 잡히는 원인을 **직접 조사**하라. 유력 후보 2가지:
  (a) **관리자 회원 조회 쿼리의 행 증식** — Prisma `include`(accounts/sessions/compatibilities 등) 로 1:N 조인이 걸려 사용자 1명이 여러 행으로 나옴. → 사용자 단위로 **distinct/그룹화**하거나 `select`만 사용해 1행/사용자 보장. Total Users 카운트도 distinct 사용자 기준으로.
  (b) **실제 중복 User 레코드** — 같은 사람이 카카오·네이버·구글로 각각 로그인해 이메일이 달라 별도 User 가 생성됨(계정 연동 없음). 이 경우 목록·카운트는 그대로 두되, 무엇이 원인인지 보고하라. (계정 연동/병합은 범위 밖 — 판단만.)
- DB 실제 행과 쿼리를 함께 확인해 (a)인지 (b)인지 판별하고, (a)면 수정, (b)면 원인과 권고를 보고.

## 4. 그 외 K-Destiny 잔재 (catch-all)
- 관리자 화면 및 로그인 후 영역(대시보드 등)에 남은 영문/K-Destiny/우주·운명 문구를 grep 해 콩닥 한국어로 교체.
- grep 예: `grep -rniE "k-?destiny|command center|premium subscriber|우주|운명|master" app components`
- 사용자 도달 가능한 곳에 잔재 0을 목표로.

## 완료 기준
- 네비: 관리자 계정에서도 상단 안 잘림, 관리자 버튼 1개.
- 어드민: 한국어 콩닥 톤 + 콩닥 컬러, 영문/K-Destiny 문구 0.
- 회원 목록: 중복 원인 규명 + (쿼리 문제면)1인 1행, 카운트 정확.
- `npm run build` 통과. 변경/조사 결과 요약 보고.
