# 콩닥 디자인 정비 — Gemini 핸드오프 (3.1 Pro 권장)

**모델:** 3.1 Pro (디자인·구조 작업이라 Flash 비권장).
**참고:** 저장소 루트 `콩닥_브랜드보드.html` 의 컬러/톤을 기준으로. 아래 토큰과 브랜드보드가 다르면 브랜드보드 우선.
**시점:** Opus5 의 배포 3대 수정(og:url·K이벤트·i18n 폴백)이 커밋된 뒤에 이 작업을 시작하라(파일 충돌 방지).
**절대 규칙:** en/de/es/fr/ja.json 등 로케일 텍스트 파일은 건드리지 말 것. 결제/매칭 로직 신규 추가 금지. 궁합 엔진(lib/compatibility.ts) 손대지 말 것.

---

## 배경(현재 문제 — 실측)
- `app/globals.css` 의 기본 테마가 **K-Destiny 다크**(`--background:#050505`, `--gold:#D4AF37`, 퍼플). `body` 가 이 다크를 상속.
- 랜딩(`app/[locale]/page.tsx`)·입력(`CompatNewClient`)만 `bg-[#FFF6F1]` 로 **국소 덮어쓰기** → 밝게 보임.
- 약관/개인정보(`app/[locale]/terms|privacy/page.tsx`)는 `bg-background`+`text-white`+gold 를 써서 **다크로 남음** → 랜딩과 완전히 따로 놈.
- 상단 네비(`components/Navbar.tsx`)가 `bg-black/95` (검정) + 6개국 언어 드롭다운 + 구글 전용 `LoginButton`.
- 소셜 로그인은 `components/AuthModal.tsx` 에 **카카오·네이버·구글·이메일이 이미 다 구현**돼 있으나, 네비가 이 모달 대신 구글 전용 버튼을 띄워 구글만 노출됨.
- 데스크톱 전용 레이아웃 부재 → 넓은 화면에 좁은 모바일 칼럼만 중앙 정렬.

## 콩닥 브랜드 토큰
- cream(배경) #FFF6F1 · ink(본문) #2B2430 · coral #FF5C77 · coral-light #FF8AA1 · plum #6A2C70 · gold #FFC24B
- 시그니처 그라데이션: #FF8AA1 → #FF5C77 → #6A2C70 (CTA·결과카드·공유카드 강조용)

---

## Part 1 — 테마를 콩닥 라이트로 통일 (근본 수정)
1. `app/globals.css` `:root` 토큰을 라이트로 재정의:
   - `--background:#FFF6F1; --foreground:#2B2430;`
   - 브랜드 토큰 추가: `--coral:#FF5C77; --coral-light:#FF8AA1; --plum:#6A2C70; --gold:#FFC24B; --cream:#FFF6F1; --ink:#2B2430;`
   - `@theme inline` 에 위 색을 노출(color-coral/plum/cream/ink, gold=#FFC24B). `body` 배경·글자색도 라이트로.
   - 다크 전용 잔재 정리: `select option{background:#0a0a0a}` → 라이트, 스크롤바 골드 톤 → coral 계열, `.bg-stardust`(흰 점)·`.text-glow-gold`·`.bg-glow-purple` 는 라이트 배경에서 안 보이거나 어색하므로 **사용처에서 제거**(특히 약관/개인정보 배경 장식).
2. 다크 하드코딩 컴포넌트를 라이트로 정리(텍스트·배경 대비 확보):
   - `terms/page.tsx`·`privacy/page.tsx`: `bg-background`·`text-white`·`text-gold`·`bg-white/[0.02]` → 크림 배경 + ink 본문 + coral/plum 제목 + 옅은 카드(예: 흰 카드 `bg-white` + `border-[#2B2430]/8` + soft shadow). 표·리스트 가독성 유지. **면책(제3조)** 강조 박스는 coral 톤으로 유지.
   - `components/Footer.tsx`: 검정 → 크림/화이트 톤. 링크·help@·하단 고지 그대로, 색만 라이트에 맞게(ink/코럴 링크).
3. **로그인 영역(dashboard/admin/login 등 회원 전용)은 Phase A 바이럴 표면이 아니다.** 라이트 전환으로 이 페이지들이 깨지면, 해당 페이지에 한해 기존 스타일을 로컬로 유지(스코프)해서 기능만 보존하라. 공개 표면(랜딩·/compat·/terms·/privacy·/guide·네비·푸터)의 라이트 통일이 최우선.

## Part 2 — 상단 네비 & 로그인 정비
1. `components/Navbar.tsx` 를 라이트로:
   - 배경 `bg-black/95` → `bg-[#FFF6F1]/80 backdrop-blur-md border-b border-[#2B2430]/8` (크림 반투명).
   - **좌측에 콩닥 브랜드**(KongdakMascot size 28~32 + "콩닥 kongdak" 워드마크)를 추가하고 홈 링크. 우측에 액션. (현재 justify-end 로 로고 없음)
2. **언어 드롭다운 제거(Phase A 한국 전용):** 네비에서 LanguageSelector/언어 select 렌더링 제거. 컴포넌트 파일은 삭제 말고 동면(미렌더).
3. **로그인 = AuthModal 로 연결(가장 중요):**
   - 네비의 구글 전용 `LoginButton` 대신, 은은한 온브랜드 **"로그인"** 버튼(고스트/아웃라인, coral 테두리)을 두고 클릭 시 `AuthModal` 을 연다. AuthModal 에는 이미 **카카오→네이버→구글→이메일**이 있으니 그대로 노출(카카오 우선). Phase A 핵심 루프는 무로그인이므로 로그인은 보조 액션으로 작게.
   - 로그인 상태에선 아바타 + 대시보드 링크 유지하되 라이트 톤으로 재색.
   - 각 소셜 버튼은 공식 브랜드색 준수: 카카오 #FEE500(검정 글씨), 네이버 #03C75A(흰 글씨), 구글 흰 배경+테두리. (AuthModal 이 이미 그렇게 돼 있으면 유지·미세정리만.)
4. `app/[locale]/layout.tsx` 의 `main pt-24 md:pt-28` 는 네비 높이 바뀌면 그에 맞게 조정.

## Part 3 — 데스크톱/모바일 각각 최적화 (반응형)
"모바일 칼럼을 PC에 늘리지" 말고, 브레이크포인트로 기기별 레이아웃을 준다.
1. 랜딩 `components/KongdakHero.tsx`:
   - 데스크톱(lg+): **2단 레이아웃** — 좌측 헤드라인+서브카피+CTA+"회원가입 없이 30초" 신뢰문구 / 우측 샘플 결과카드(91점)+콩닥이. `max-w-6xl` 정도로 넓게, 데스크톱 타이포 스케일 확대.
   - 모바일: 세로 스택, 중앙 정렬(현재 톤 유지).
2. 입력 `components/CompatNewClient.tsx`:
   - 데스크톱: 편안한 카드 폭(`max-w-2xl`)과 여백. 넓은 화면에선 "내 정보 / 상대방 정보"를 **2열 병치** 고려. 모바일: 1열 스택.
3. 결과 `components/CompatResultClient.tsx`·`/terms`·`/privacy`·`/guide`: 데스크톱 가독 폭(`max-w-3xl` 내외) + 여백 정리. 모바일 유지.
4. 공통: `sm/md/lg` 브레이크포인트로 폰트·간격·컨테이너 폭을 단계화. body 가 넓은 화면에서 가로 스크롤 없이, 좁은 칼럼만 덩그러니 뜨지 않게.

---

## 하지 말 것
- en/de/es/fr/ja.json 등 로케일 텍스트 파일 수정 금지(동면 유지).
- 궁합 엔진·결제·매칭 로직 손대지 말 것. 소셜 provider 설정(NextAuth) 변경 금지 — UI 연결만.
- 기존 기능(카톡 공유, K-loop, GA 이벤트) 회귀 금지.

## 완료 기준
- `npm run build` 통과, 타입 에러 0.
- 랜딩·입력·결과·약관·개인정보·네비·푸터가 **하나의 콩닥 라이트 브랜드**로 일관.
- 상단 네비가 크림 톤 + 좌측 콩닥 로고 + 언어 드롭다운 제거 + "로그인" 클릭 시 카카오/네이버/구글/이메일 모달.
- 데스크톱에서 넓은 2단/여백 레이아웃, 모바일에서 세로 스택 — 각 기기에 최적화.
- 변경/생성 파일 목록과, 데스크톱·모바일 스크린샷(가능하면) 보고.
```
